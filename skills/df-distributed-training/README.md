# Distributed Training

Scale model training across multiple GPUs and machines.

## What you can do
- Train on large datasets that don't fit in one GPU
- Speed up training 5-10x with multiple GPUs
- Parallelize feature engineering across columns
- Search 100+ hyperparameter configurations in hours
- Optimize batch sizes and gradient communication
